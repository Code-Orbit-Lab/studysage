# Integration Tests

Cross-service tests that don't belong to any single service's own test
suite (which each own their unit tests inside `backend/`, `frontend/`,
`ai-service/`). Requires all three services running locally.
